// const gallery = document.querySelector('.gallery')
// const products = document.querySelectorAll('.gallery .product')

// function updateGalleryMaxWidth(){
//     const lasItem = gallery.querySelector('.product:last-child')
//     const totalWidth = lasItem.getBoundingClientRect().x + lasItem.offsetWidth

//     gallery.style.maxWidth = `${totalWidth}px`
// }

// updateGalleryMaxWidth()

// window.addEventListener('resize', updateGalleryMaxWidth)

